'use strict';

describe('myApp.roomDetail module', function() {

  beforeEach(module('myApp.roomDetail'));

  describe('roomDetail controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});